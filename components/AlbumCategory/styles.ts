import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 10,
    },

    title: {
        color: 'white',
        fontSize: 30,
        marginLeft: 10,
        margin: 10,
        fontWeight: 'bold',
    }
})

export default styles;